class SurahIndexEntry {
  const SurahIndexEntry({
    required this.number,
    required this.name,
    required this.startPage,
  });

  final int number;
  final String name;
  final int startPage;
}
