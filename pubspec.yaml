name: flutter_bloc_bp
description: Flutter Boiler plate using Bloc
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1
flutter-version: 3.47.2

environment:
  sdk: ^3.13.0
  flutter: ">=3.47.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  shared_preferences: ^2.5.2
  nested: ^1.0.0
  flutter_screenutil: ^5.9.3
  http_mock_adapter: ^0.6.1
  go_router: ^17.0.1
  flutter_bloc: ^9.1.1
  bloc: ^9.1.0
  pretty_dio_logger: ^1.4.0
  dio: ^5.8.0+1
  firebase_crashlytics: ^5.0.6
  firebase_core: ^4.3.0
  firebase_analytics: ^12.1.0
  email_validator: ^3.0.0
  mocktail: ^1.0.4
  network_image_mock: ^2.1.1
  dio_cache_interceptor_hive_store: ^3.2.2
  web_socket_channel: ^3.0.1
  equatable: ^2.0.5
  network_flutter:
    path: mobile_api_client
  

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  win32: ^5.5.4
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  fonts:
    # Satoshi — referenced by AppTheme / SatoshiTextStyles. Add the Satoshi
    # .otf/.ttf files under assets/fonts/ and fill this in; until then 'Satoshi'
    # falls back to the platform default font.
    # - family: Satoshi
    #   fonts:
    #     - asset: assets/fonts/Satoshi-Light.otf
    #       weight: 300
    #     - asset: assets/fonts/Satoshi-Regular.otf
    #       weight: 400
    #     - asset: assets/fonts/Satoshi-Medium.otf
    #       weight: 500
    #     - asset: assets/fonts/Satoshi-Bold.otf
    #       weight: 700
    #     - asset: assets/fonts/Satoshi-Black.otf
    #       weight: 900
    - family: Geist
      fonts:
        - asset: assets/fonts/static/Geist-Thin.ttf
          weight: 100
        - asset: assets/fonts/static/Geist-ThinItalic.ttf
          weight: 100
          style: italic
        - asset: assets/fonts/static/Geist-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/static/Geist-ExtraLightItalic.ttf
          weight: 200
          style: italic
        - asset: assets/fonts/static/Geist-Light.ttf
          weight: 300
        - asset: assets/fonts/static/Geist-LightItalic.ttf
          weight: 300
          style: italic
        - asset: assets/fonts/static/Geist-Regular.ttf
          weight: 400
        - asset: assets/fonts/static/Geist-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/static/Geist-Medium.ttf
          weight: 500
        - asset: assets/fonts/static/Geist-MediumItalic.ttf
          weight: 500
          style: italic
        - asset: assets/fonts/static/Geist-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/static/Geist-SemiBoldItalic.ttf
          weight: 600
          style: italic
        - asset: assets/fonts/static/Geist-Bold.ttf
          weight: 700
        - asset: assets/fonts/static/Geist-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: assets/fonts/static/Geist-ExtraBold.ttf
          weight: 800
        - asset: assets/fonts/static/Geist-ExtraBoldItalic.ttf
          weight: 800
          style: italic
        - asset: assets/fonts/static/Geist-Black.ttf
          weight: 900
        - asset: assets/fonts/static/Geist-BlackItalic.ttf
          weight: 900
          style: italic

  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
